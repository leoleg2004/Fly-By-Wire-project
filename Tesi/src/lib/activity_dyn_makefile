CC = gcc
CFLAGS = -g -Wall -O2

all: activity_library_dyn.a

activity_library_dyn.a: activity_library_dyn.o
	ar rcs $@ $^

activity_library_dyn.o: activity_library_dyn.c activity_library_dyn.h
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f *.o *.a
