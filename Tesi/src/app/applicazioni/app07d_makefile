TARGET = app07d

all: $(TARGET) clean print

$(TARGET): app07d_main.o
	gcc -g -Wall -pthread  $^ -o $@  -lm
	
	
app08_main.o: app07d_main.c
	gcc -c $< -o $@
     
clean:
	rm -f *.o

print:
	@echo "to run: ./app07d <sleep1, sleep2, ...>"

