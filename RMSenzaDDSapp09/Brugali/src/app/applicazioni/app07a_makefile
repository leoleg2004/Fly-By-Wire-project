TARGET = app07a

all: $(TARGET) clean print

$(TARGET): app07a_main.o
	gcc -g -Wall -pthread  $^ -o $@  -lm
	
	
app07a_main.o: app07a_main.c
	gcc -c $< -o $@
     
clean:
	rm -f *.o

print:
	@echo "to run: ./app07a"

